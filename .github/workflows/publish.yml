name: Release Binaries for Zed Extension
on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

jobs:
  build-tarball:
    name: Build ${{ matrix.platform }} tarball
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-latest
            platform: darwin-aarch64
            binary: z-ai-acp
            archive: tar.gz
          - os: macos-13
            platform: darwin-x86_64
            binary: z-ai-acp
            archive: tar.gz
          - os: ubuntu-latest
            platform: linux-x86_64
            binary: z-ai-acp
            archive: tar.gz
          - os: windows-latest
            platform: windows-x86_64
            binary: z-ai-acp.exe
            archive: zip

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --frozen-lockfile --ignore-engines

      - name: Build TypeScript
        run: yarn build

      - name: Prepare distribution directory
        shell: bash
        run: |
          mkdir -p dist-pkg
          cp -r dist dist-pkg/
          cp -r node_modules dist-pkg/
          cp package.json dist-pkg/
          cp README.md dist-pkg/ || true
          cp icon.svg dist-pkg/ || true
          cp extension.toml dist-pkg/ || true

      - name: Create wrapper script (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          echo '#!/bin/bash' > dist-pkg/${{ matrix.binary }}
          echo 'DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"' >> dist-pkg/${{ matrix.binary }}
          echo 'exec node "$DIR/dist/index.js" "$@"' >> dist-pkg/${{ matrix.binary }}
          chmod +x dist-pkg/${{ matrix.binary }}

      - name: Create wrapper script (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          echo '@echo off' > dist-pkg/${{ matrix.binary }}
          echo 'node "%~dp0\dist\index.js" %*' >> dist-pkg/${{ matrix.binary }}

      - name: Create tar.gz archive
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: |
          cd dist-pkg
          tar -czf ../z-ai-acp-${{ matrix.platform }}.tar.gz .
          cd ..
          shasum -a 256 z-ai-acp-${{ matrix.platform }}.tar.gz | tee z-ai-acp-${{ matrix.platform }}.tar.gz.sha256

      - name: Create zip archive
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          Compress-Archive -Path dist-pkg/* -DestinationPath z-ai-acp-${{ matrix.platform }}.zip

      - name: Generate SHA256 for zip
        if: matrix.archive == 'zip' && runner.os == 'Windows'
        shell: powershell
        run: |
          $hash = (Get-FileHash -Algorithm SHA256 z-ai-acp-${{ matrix.platform }}.zip).Hash.ToLower()
          "$hash  z-ai-acp-${{ matrix.platform }}.zip" | Out-File -Encoding ASCII z-ai-acp-${{ matrix.platform }}.zip.sha256

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: z-ai-acp-${{ matrix.platform }}
          path: |
            z-ai-acp-${{ matrix.platform }}.${{ matrix.archive }}
            z-ai-acp-${{ matrix.platform }}.${{ matrix.archive }}.sha256

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [build-tarball]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          files: |
            artifacts/**/*.tar.gz
            artifacts/**/*.zip
            artifacts/**/*.sha256
          body: |
            ## 🎉 Z.AI Agent for Zed

            This release includes distribution packages for the Zed Agent Server Extension.

            ### 🚀 Installation via Zed Extension

            This extension is available in the Zed Extensions registry. The packages will be automatically downloaded when you install the extension.

            ### 📦 Distribution Format

            Starting from v0.0.5, we use tarball/zip distribution instead of single binaries for better ESM compatibility.

            ### 🖥️ Supported Platforms

            - ✅ macOS ARM64 (Apple Silicon)
            - ✅ macOS x86_64 (Intel)
            - ✅ Linux x86_64
            - ✅ Windows x86_64

            ### 🔐 Checksums

            SHA256 checksums are provided for each package to verify integrity.

            ---

            🤖 Generated with [Claude Code](https://claude.com/claude-code)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
